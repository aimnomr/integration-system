import { PagePlaceholder } from '../_PagePlaceholder';

export default function AdminMaps() {
  return (
    <PagePlaceholder
      title="Admin — Maps"
      phase="Phase 4 — coming soon"
      description="CRUD on the maps table via /maps. DataGrid + drawer edit forms (MUI). Map uploads (PGM + yaml) come later."
    />
  );
}
