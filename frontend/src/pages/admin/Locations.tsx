import { PagePlaceholder } from '../_PagePlaceholder';

export default function AdminLocations() {
  return (
    <PagePlaceholder
      title="Admin — Named Locations"
      phase="Phase 4 — coming soon"
      description="CRUD on named_locations via /locations. Pick coordinates from a map preview when creating or editing."
    />
  );
}
