import { PagePlaceholder } from '../_PagePlaceholder';

export default function AdminFleetConfig() {
  return (
    <PagePlaceholder
      title="Admin — Fleet Config"
      phase="Phase 4 — coming soon"
      description="Single-row editor for the fleet_config table (interface name, version, manufacturer). Backed by PUT /fleet — registry reloads after save."
    />
  );
}
