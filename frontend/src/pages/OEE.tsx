import { PagePlaceholder } from './_PagePlaceholder';

export default function OEE() {
  return (
    <PagePlaceholder
      title="OEE Dashboard"
      phase="Phase 4 — coming soon"
      description="Trip-cycle count, success rate, average duration; bar + line charts over time, per-robot breakdown. Built on GET /robots/{serial}/oee/* — MUI X Charts."
    />
  );
}
