import { PagePlaceholder } from './_PagePlaceholder';

export default function Orders() {
  return (
    <PagePlaceholder
      title="Order History"
      phase="Phase 4 — coming soon"
      description="Searchable paged list off GET /orders. Filter by robot and date range; click a row to replay the order path on the map view."
    />
  );
}
