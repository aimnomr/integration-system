import { PagePlaceholder } from './_PagePlaceholder';

export default function Dispatch() {
  return (
    <PagePlaceholder
      title="Dispatch"
      phase="Phase 3 — coming soon"
      description="Pick a robot, then pick destination(s) — click on the map, choose a named location, or enter x/y/θ. Build a multi-node order, preview, and send via POST /robots/{serial}/order. Active-order panel exposes Cancel / Retry / Skip."
    />
  );
}
